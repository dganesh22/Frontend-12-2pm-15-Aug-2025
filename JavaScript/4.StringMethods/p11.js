// string.endsWith(input)
 var str = "Welcome to the Javascript";

// checking the given string word or character must present at the end of the string
var check = str.endsWith("Javascript"); // regular expression is not allowed as parameter

console.log(`check =`, check); // true