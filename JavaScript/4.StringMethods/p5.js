// string join

 var str = "Hello, this is javascript logic";

        console.log(`str =`, str);

        let out = str.split(" ");
        console.log(`str split =`,out);
        
        console.log(`str join =`, out.join("-"));